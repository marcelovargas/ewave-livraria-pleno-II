@model IEnumerable<Entities.Emprestimo>

@{
    ViewData["Title"] = "Emprestimos";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create" class="btn btn-outline-success"><i class="fa fa-file"></i></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DFIm)
            </th>
            @*<th>
                    @Html.DisplayNameFor(model => model.Letor)
                </th>*@
            <th>
                @Html.DisplayNameFor(model => model.Livro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.DInicio)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DFIm)
                </td>
                @*<td>
                        @Html.DisplayFor(modelItem => item.Letor.Id)
                    </td>*@
                <td>
                    @Html.DisplayFor(modelItem => item.Livro.Id)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-warning"><i class="fa fa-edit"></i></a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-info"><i class="fa fa-eye"></i></a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger"><i class="fa fa-trash"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
